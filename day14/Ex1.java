class Ex1
{
    public static void main(String[] args)
    {

        //int[] x=new int[3];
        //double[] x=new double[3];
        //boolean[] x=new boolean[3];//boolean預設值是false
        //String[] x=new String[3];//String預設值是null
        //Double[] x=new Double[3];//Double預設值是null
        Boolean[] x=new Boolean[3];
        //double Double ; boolean Boolean ; 大小寫不一樣

        //new給的初始值,小寫的都是給基本值0,大寫的都是給物件值null

        //System.out.println(x);
        System.out.println(x+"\t"+x[0]+"\t"+x[1]+"\t"+x[2]);//int整數第一個是0
        //變數要存在一定要給值(value)

        /*
        []中括號後面的x代表"一整組(很多個)"的變數名稱,類型是int,
        後面[]中括號裡的的數字代表x有幾個
        左右都要有中括號,代表類型一致
        */




        /*
       int abc=10;
       int def=20;
       int ghi=30;

       System.out.println(abc+def+ghi); 
       */

    }
}